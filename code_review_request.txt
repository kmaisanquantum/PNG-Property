I have split the unified Render service into two separate services:
1. 'png-property-api' (FastAPI Web Service via Docker)
2. 'png-property-dashboard' (React Static Site)

Key changes:
- Updated 'render.yaml' to define both services.
- Configured 'VITE_API_URL' in the static site to automatically pull the URL from the API service.
- Verified 'backend/Dockerfile' is ready for standalone operation.
- Verified 'frontend/public/_redirects' is present for SPA routing.
- Confirmed that 'frontend' can still build successfully.
